{
  "image": "mcr.microsoft.com/devcontainers/base:jammy",
  "containerEnv": {
    "GH_TOKEN": "${localEnv:GH_TOKEN}",
    "OPENAI_API_KEY": "${localEnv:OPENAI_API_KEY}",
    "PPLX_API_KEY": "${localEnv:PPLX_API_KEY}"
  }
}
